Created with built-in image generation. Final PNG: foreground-three-cubes-v2.png.
Three larger cubes for the upper key focus area; place the bottom edge above the STACKS title.

Generation prompt:
Use case: precise-object-edit. Create a new standalone foreground sprite for the STACKS game tile, using the attached six-cube tower as the exact material and visual identity reference. Replace the tall thin six-cube tower with EXACTLY THREE large chunky crystal cubes in a compact slightly staggered upright stack. Bottom cube ice cyan, middle cube cyan-violet, top cube rich violet. Preserve transparent glass, bright beveled edges, small faceted diamond cores inside each cube, and premium crisp three-quarter-view 3D render. Bottom and middle cubes touch as a stable stack, top cube floats only a tiny gap above middle. Slight alternating rotations, all three cubes fully visible, strong broad silhouette, stack total width about 65% of total height, NOT a thin tall column. Square canvas, entire stack occupies about 85 percent of height, centered with clean margins and no cropping. Genuine transparent alpha background with completely empty pixels around cubes. No background color, no black backdrop, no checkerboard baked in, no platform, no floor, no cast ground shadow, no rings, no stars, no text, no logo, no title. Tight controlled edge glow only. This asset must be directly usable as the foreground in a tile editor and recognizable at small thumbnail size.

Transparency correction prompt:
Use case: background-extraction. Edit this image: remove the entire white and gray checkerboard background and the tiny stray dot at upper left. Output the exact same THREE crystal cubes as a clean isolated cutout on a REAL TRANSPARENT ALPHA CHANNEL. Transparent means alpha=0 outside the silhouette, not a drawn checkerboard or a white or black background. Preserve the three cubes, their colors, positions, internal diamond cores, fine bright edges and square canvas unchanged. No additional elements. This is a production foreground PNG for compositing.
